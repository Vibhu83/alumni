Color coding (Day mode){

}

Register details{
Profile photo(optional)
ID-field(6 digits)
Name-field
Email(field)
Password(field) with a meter for how strong the password is
Batch Year(dropdown with all the years)
Course(drop down with all the courses)
Alumni Toggle, when ON{
	Changes the Register button to Next button, clicking which shows the following form:
	Current profession(text field)
	Employement status(Radio buttons): Employed, Self-employed, not employed
	if Employed{
		Current company(text)
		Current position(text)
		}
	}
}
